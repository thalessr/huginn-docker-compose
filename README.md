# huginn-docker-compose
A docker compose file for Huginn project

# Usage
### Starting
* Build images & starts the containers `docker-compose up`
* Build images & starts the containers in the background `docker-compose up -d`
### Stopping
`docker-compose down`
